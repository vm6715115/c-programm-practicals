#include<stdio.h>
int main()
{
    int a =1 , b ;
    // printf("enter the num a");
    // scanf("%d", &a);
      printf("enter the num  b");
     scanf("%d", &b);
    while (a<=b)
    {
        if ( a%2 == 0)
        {
              printf("%d\n", a );
        }
        
        
        a++;
    }
    
}